# Anaconda configuration

Some details can be found [here](https://conda.io/projects/conda/en/latest/user-guide/configuration/use-condarc.html)

On Windows `.condarc` file should be put into `$Env:userprofile` (or
`%USERPROFILE%`) path.

It is just simple `yaml` file.
